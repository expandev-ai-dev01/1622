import { Router } from 'express';

const router = Router();

/**
 * @summary External API routes configuration
 * @description Public endpoints that do not require authentication
 */

/**
 * @api External routes will be added here as features are implemented
 * Example structure:
 * - /api/v1/external/security/login
 * - /api/v1/external/public/...
 */

export default router;
