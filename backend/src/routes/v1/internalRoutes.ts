import { Router } from 'express';

const router = Router();

/**
 * @summary Internal API routes configuration
 * @description Authenticated endpoints that require valid user session
 */

/**
 * @api Internal routes will be added here as features are implemented
 * Example structure:
 * - /api/v1/internal/record
 * - /api/v1/internal/user
 */

export default router;
